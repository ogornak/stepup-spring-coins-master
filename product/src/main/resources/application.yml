server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/adm
    username: adm
    password: adm
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
